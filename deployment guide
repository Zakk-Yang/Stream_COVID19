
Commit the project to Github:
VCS --> Import into version control --> Share project on Github


Commit the file when code is changed:
click the tick on the top right icon (local commit) --> command_shift+k to push to github (remote commit)

Open Heroku online account:

in the project terminal type: pip freeze > requirements.txt
this will be helpful for Heroku to detect the language

